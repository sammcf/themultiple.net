.DS_Store

# The design system specimen lives in docs/ and is deliberately not committed.
# Everything on this branch is served at themultiple.net, and the specimen is
# working material rather than a page of the site. Keep it local.
/docs/
